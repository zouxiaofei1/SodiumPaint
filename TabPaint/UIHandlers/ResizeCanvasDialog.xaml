<Window x:Class="TabPaint.ResizeCanvasDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="调整画布大小" 
        Height="240" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" 
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Segoe UI Variable, Segoe UI, Microsoft YaHei UI"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- 主色调 (Fluent Blue) -->
        <SolidColorBrush x:Key="SystemAccentColor" Color="#0067C0"/>

        <!-- 通用 TextBox 样式 (保持不变) -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#B0B0B0"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource SystemAccentColor}"/>
                                <Setter Property="BorderThickness" TargetName="border" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 主按钮样式 -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SystemAccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 次要按钮样式 -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="border" Value="#F0F0F0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" TargetName="border" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- 窗口主容器 (去掉了阴影 Effect) -->
    <Border Background="White" CornerRadius="8" BorderBrush="#C0C0C0" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <!-- 标题栏 -->
                <RowDefinition Height="*"/>
                <!-- 内容区域 -->
                <RowDefinition Height="Auto"/>
                <!-- 底部按钮 -->
            </Grid.RowDefinitions>

            <!-- 1. 标题栏 (Distinct Color + Drag Event) -->
            <!-- CornerRadius 上方圆角为8，下方为0，贴合窗口 -->
            <Border Grid.Row="0" Background="#F3F3F3" CornerRadius="7,7,0,0" 
                    BorderBrush="#E5E5E5" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 你的图标 -->
                    <Image x:Name="AppIcon" 
                           Grid.Column="0"
                           Source="pack://application:,,,/Resources/TabPaint.ico" 
                           Width="16" Height="16" 
                           Margin="12,0,8,0"
                           VerticalAlignment="Center"
                           RenderOptions.BitmapScalingMode="HighQuality"/>

                    <TextBlock Grid.Column="1" Text="调整画布大小" VerticalAlignment="Center" 
                               FontWeight="SemiBold" FontSize="14" Foreground="#333333"/>
                </Grid>
            </Border>

            <!-- 2. 内容区域 (White Background) -->
            <Grid Grid.Row="1" Margin="24,10,24,10" VerticalAlignment="Center" Background="White">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <!-- 间距 -->
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>

                <!-- 宽度行 -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="宽度" VerticalAlignment="Center" Foreground="#555555"/>
                <TextBox x:Name="WidthTextBox" Grid.Row="0" Grid.Column="1" />
                <TextBlock Grid.Row="0" Grid.Column="2" Text="px" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="#888888"/>

                <!-- 高度行 -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="高度" VerticalAlignment="Center" Foreground="#555555"/>
                <TextBox x:Name="HeightTextBox" Grid.Row="2" Grid.Column="1" />
                <TextBlock Grid.Row="2" Grid.Column="2" Text="px" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="#888888"/>
            </Grid>

            <!-- 3. 底部按钮区域 (White Background matching Content) -->
            <Border Grid.Row="2" Background="White" Padding="16" CornerRadius="0,0,8,8">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="OkButton" Content="确定" Width="80" IsDefault="True" Click="OkButton_Click" 
                            Style="{StaticResource PrimaryButtonStyle}"/>
                    <Button x:Name="CancelButton" Content="取消" Width="80" Margin="10,0,0,0" IsCancel="True"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
